import * as Styled from "./styles";

const Profile = () => {
  return (
    <Styled.ProfileContainer>
      <Styled.Frame></Styled.Frame>
    </Styled.ProfileContainer>
  );
};

export default Profile;
