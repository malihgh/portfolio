import { createTheme } from "@mui/material";

export const customTheme = createTheme({
  palette: {
    primary: {
      main: "#2FBFBF",
    },
    secondary: {
      main: "#FFB400",
    },
  },
});
